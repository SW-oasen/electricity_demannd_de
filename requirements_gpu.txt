# ============================================================
# Requirements for GPU / CUDA machine
# Target: SARIMAX on hourly data + GPU-accelerated tree models
#
# Tested with Python 3.11 / 3.12 (recommended for CUDA compat.)
# CUDA 12.x required for cupy-cuda12x and GPU tree models
#
# Install base deps:
#   pip install -r requirements_gpu.txt
#
# For LightGBM with GPU support, build from source or use
# the pre-built wheel (needs CUDA + CMake):
#   pip install lightgbm --config-settings=cmake.define.USE_CUDA=ON
#   -- OR just use device='cuda' with the standard wheel
#      (supported since LightGBM >= 4.0)
#
# XGBoost GPU: no special build needed; set device='cuda' in
#   XGBRegressor(device='cuda', ...)
# ============================================================

# --- Core data stack ----------------------------------------
numpy==2.4.4
pandas==3.0.2
scipy==1.17.1
statsmodels==0.14.6          # SARIMAX – CPU only, but fast on many cores
patsy==1.0.2

# --- Machine learning ---------------------------------------
scikit-learn==1.8.0
joblib==1.5.3
threadpoolctl==3.6.0
sklearn-compat==0.1.5
imbalanced-learn==0.14.1
shap==0.51.0
slicer==0.0.8

# --- GPU-accelerated tree models ----------------------------
xgboost>=2.1.0               # use XGBRegressor(device='cuda', ...)
lightgbm>=4.5.0              # use LGBMRegressor(device='cuda', ...)

# --- Optional: GPU-accelerated numpy-compatible arrays ------
# cupy-cuda12x>=13.0.0       # uncomment if CUDA 12.x is installed
#                            # enables fast GPU array ops (cupy.ndarray)
# cuml-cu12                  # RAPIDS cuML: GPU sklearn-compatible models
#                            # install via: pip install cuml-cu12
#                            # (requires RAPIDS-compatible CUDA + driver)

# --- Visualisation ------------------------------------------
matplotlib==3.10.9
seaborn==0.13.2
contourpy==1.3.3
cycler==0.12.1
fonttools==4.62.1
kiwisolver==1.5.0
pillow==12.2.0
pyparsing==3.3.2

# --- Jupyter / notebook -------------------------------------
ipykernel==7.2.0
ipython==9.13.0
ipython_pygments_lexers==1.1.1
jupyter_client==8.8.0
jupyter_core==5.9.1
comm==0.2.3
nest-asyncio==1.6.0
matplotlib-inline==0.2.1
debugpy==1.8.20

# --- Utilities ----------------------------------------------
requests==2.33.1
beautifulsoup4==4.14.3
tqdm==4.67.3
numba==0.65.1                # JIT acceleration; keep in sync with llvmlite
llvmlite==0.47.0
cloudpickle==3.1.2
colorama==0.4.6
python-dateutil==2.9.0.post0
pyzmq==27.1.0
six==1.17.0
soupsieve==2.8.3
tzdata==2026.2
urllib3==2.6.3
certifi==2026.4.22
charset-normalizer==3.4.7
idna==3.13
packaging==26.2
platformdirs==4.9.6
psutil==7.2.2
tornado==6.5.5
traitlets==5.14.3
typing_extensions==4.15.0
wcwidth==0.7.0

# --- IPython helpers ----------------------------------------
asttokens==3.0.1
decorator==5.2.1
executing==2.2.1
jedi==0.20.0
parso==0.8.7
prompt_toolkit==3.0.52
pure_eval==0.2.3
Pygments==2.20.0
stack-data==0.6.3
